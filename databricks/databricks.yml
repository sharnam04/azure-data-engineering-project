bundle:
  name: spotify_dab
  uuid: b9268ef6-e533-4af9-8eee-5cff04cb26b1

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405618029728139.19.azuredatabricks.net
      
    presets: 
      source_linked_deployment: false
    variables:
      catalog: catalog
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-7405618029728139.19.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/kansalsharnam0403@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: catalog
      schema: prod
    permissions:
      - user_name: kansalsharnam0403@gmail.com
        level: CAN_MANAGE
